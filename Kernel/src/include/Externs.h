#pragma once
#ifndef H_Externs
#define H_Externs
#include <Types.h>
/*
	External declarations for functions / symbols 
	defined in assembly (this is temporary)
*/

global void memset64(vptr dest, uint64 value, nint numBytes);

#endif